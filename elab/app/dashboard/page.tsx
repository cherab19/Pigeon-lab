import { requireUser } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
import Link from "next/link";
export default async function DashboardPage() { const user = await requireUser(); const profile = await prisma.profile.findUnique({ where: { userId: user.id }, include: { school: true } }); return <main className="min-h-screen bg-background"><header className="border-b bg-card"><div className="container mx-auto flex items-center justify-between p-4"><Link href="/" className="font-display font-bold text-xl">Pigeonlab</Link><form action="/api/auth/signout" method="post"><button className="text-sm text-muted-foreground">Sign out</button></form></div></header><section className="container mx-auto p-6"><h1 className="font-display text-3xl font-bold">Welcome, {profile?.fullName || user.email}</h1><p className="mt-2 text-muted-foreground">{profile?.school?.name || "Your Pigeonlab account"}</p><div className="mt-8 grid gap-4 md:grid-cols-3"><Link className="rounded-xl border bg-card p-5 hover:shadow-card" href="/lab">Virtual labs</Link><Link className="rounded-xl border bg-card p-5 hover:shadow-card" href="/textbooks">Textbooks</Link><Link className="rounded-xl border bg-card p-5 hover:shadow-card" href="/manage-users">Manage users</Link></div></section></main>; }
